base = float(input('Digite o valor da base do triângulo em cm: '))
altura = float(input('Digite o valor da altura do triângulo em cm: '))
area = float((base * altura) / 2)

print(f'A área do triângulo retângulo é de {area:.2f} cm*2.')